---
import BaseLayout from '@valesordev/ui/BaseLayout.astro';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const { title = 'bashburn', description } = Astro.props;
---

<BaseLayout {title} {description} favicon="/favicon.ico">
  <slot />
</BaseLayout>
